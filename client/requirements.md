## Packages
framer-motion | Animations for menu items entering
lucide-react | Icons for UI elements (already in base, but emphasizing usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
